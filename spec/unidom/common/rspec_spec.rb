require 'spec_helper'

describe Unidom::Common::Rspec do
  it 'has a version number' do
    expect(Unidom::Common::Rspec::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
